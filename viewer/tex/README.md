Фото-фактуры Poly Haven (https://polyhaven.com), лицензия CC0. Разрешение 1K; карты: `_diff` (цвет, у большинства
переведён в серый — цвет задаёт материал), `_nor` (нормали OpenGL), `_rough` (шероховатость, 512 px).
Источник каждой фактуры, её размер в метрах и авторы — в `textures.json`.
